# grouse cli

Native command-line client: a Rust TUI.

Consumes the grouse core directly as a Rust crate (no FFI). It renders the shared
design tokens (`design/`) as terminal color/spacing where applicable.
