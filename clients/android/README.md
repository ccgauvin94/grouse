# grouse android

Native Android client: Kotlin + Jetpack Compose.

Consumes the grouse core via the uniffi-generated Kotlin bindings and renders the
shared design tokens (`design/`) natively in Compose. No client logic lives here.
