# grouse desktop

Native Linux desktop client: Qt6 + KF6 Kirigami (QML/C++).

Consumes the grouse core via its C ABI and renders the shared design tokens
(`design/`) natively in Kirigami. No client logic lives here — the desktop is a
thin UI over the core.
